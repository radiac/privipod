version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"

sphinx:
   configuration: docs/conf.py
   builder: dirhtml

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs
